<h2>投稿詳細</h2>

<table>
  <thead>
    <tr>
      <th>タイトル</th>
      <th>投稿内容</th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td><p><%= @post.title %></p></td>
      <td><p><%= @post.content %></p></td>
    </tr>
  </tbody>
</table>


<%= link_to '編集', edit_post_path(@post)%>
        
<%= link_to "一覧へ", posts_path %>

<td><%= button_to '削除', post_path(@post), method: :delete, data: { turbo_confirm: "本当に削除しますか？" } %></td>
